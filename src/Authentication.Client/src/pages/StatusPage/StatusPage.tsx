import React from 'react';

import styles from './StatusPage.module.scss';

const StatusPage = () => {
    return (
        <div className={styles.StatusForm}>
            <div className={styles.StatusText}>Success</div>
        </div>
    );
};

export default StatusPage;
